import type { Dataset } from "../types/dataset.ts";

/**
 * TODO: verify against CDC/NCHS Vital Statistics Rapid Release, Provisional Drug Overdose Data.
 *
 * Generated by scripts/generate-datasets.ts from a handful of real, sourced
 * anchor points, piecewise-linearly interpolated (lib/datasets/interpolate.ts)
 * into an annual series. The endpoints and named inflection years are real;
 * everything interpolated between them is NOT - a human needs to pull the
 * exact annual series before this ships. verified stays false and isActive
 * stays false until then (see scripts/validate-datasets.ts).
 *
 * Cause-of-death coding for specific drug categories has been revised multiple times; a meaningful share of death certificates historically lacked a specified drug (varies by state/year). Provisional counts are later revised upward as lagging reports arrive.
 */
export const usDrugOverdoseDeaths: Dataset = {
  slug: "us-drug-overdose-deaths",
  title: "US drug overdose death rate, 1999-2024",
  question: "Overdose deaths per 100,000 people. Draw what happened after 2014.",
  yLabel: "Drug overdose deaths per 100,000 population",
  yUnit: "per 100,000",
  xValues: ["1999","2000","2001","2002","2003","2004","2005","2006","2007","2008","2009","2010","2011","2012","2013","2014","2015","2016","2017","2018","2019","2020","2021","2022","2023","2024"],
  yValues: [6.1,6.7,7.2,7.8,8.3,8.9,9.4,10,10.5,11.1,11.6,12.2,12.7,13.3,13.8,16.1,18.5,20.8,23.1,25.4,27.8,30.1,32.4,31.9,31.3,24],
  revealFromIndex: 15,
  yDomain: [0, 35],
  sourceName: "CDC/NCHS Vital Statistics Rapid Release, Provisional Drug Overdose Data",
  sourceUrl: "https://www.cdc.gov/nchs/nvss/vsrr/drug-overdose-data.htm",
  verified: false,
  verifiedOn: null,
  isActive: false,
  // Rated red because the values between the anchor points were produced
  // by a straight line, not measured by anyone. See the reliability note
  // in supabase/migrations/20260827160000_reliability.sql.
  reliability: "red",
  reliabilityNote:
    "Values are piecewise-linear interpolations between a small number of real, sourced anchor points (see lib/datasets/interpolate.ts), not the source's own annual series. Re-import exact values before publishing "
    + "anything from this chart.",
};
