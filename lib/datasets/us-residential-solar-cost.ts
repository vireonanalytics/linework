import type { Dataset } from "../types/dataset.ts";

/**
 * TODO: verify against National Renewable Energy Laboratory (NREL), U.S. Solar PV Cost Benchmark.
 *
 * Generated by scripts/generate-datasets.ts from a handful of real, sourced
 * anchor points, piecewise-linearly interpolated (lib/datasets/interpolate.ts)
 * into an annual series. The endpoints and named inflection years are real;
 * everything interpolated between them is NOT - a human needs to pull the
 * exact annual series before this ships. verified stays false and isActive
 * stays false until then (see scripts/validate-datasets.ts).
 *
 * NREL revised its bottom-up benchmarking methodology at points in the series (component pricing, labor-cost modeling). Figures are national averages and vary substantially by state/incentive regime.
 */
export const usResidentialSolarCost: Dataset = {
  slug: "us-residential-solar-cost",
  title: "Cost of installing residential solar, 2010-2023",
  question: "National average unsubsidized cost to install residential solar, dollars per watt. Draw what happened after 2018.",
  yLabel: "Installed cost per watt-DC, before incentives",
  yUnit: "$/watt",
  xValues: ["2010","2011","2012","2013","2014","2015","2016","2017","2018","2019","2020","2021","2022","2023"],
  yValues: [7.5,6.9,6.3,5.7,5,4.4,3.8,3.7,3.5,3.4,3.2,3.1,2.9,2.8],
  revealFromIndex: 8,
  yDomain: [0, 8],
  sourceName: "National Renewable Energy Laboratory (NREL), U.S. Solar PV Cost Benchmark",
  sourceUrl: "https://docs.nrel.gov/docs/fy23osti/87303.pdf",
  verified: false,
  verifiedOn: null,
  isActive: false,
  // Rated red because the values between the anchor points were produced
  // by a straight line, not measured by anyone. See the reliability note
  // in supabase/migrations/20260827160000_reliability.sql.
  reliability: "red",
  reliabilityNote:
    "Values are piecewise-linear interpolations between a small number of real, sourced anchor points (see lib/datasets/interpolate.ts), not the source's own annual series. Re-import exact values before publishing "
    + "anything from this chart.",
};
